let str = "bangladesh is my country";
let result = "";

let words = str.split(" ");

for (let i = 0; i < words.length; i++) {
    let word = words[i];

    result += word[0].toUpperCase() + word.slice(1);

    if (i !== words.length - 1) {
        result += " ";
    }
}

console.log(result);
