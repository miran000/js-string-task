var name = "BAngladesh";
var count = 0; 
for (var i = 0; i < name.length; i++) {
    if (name[i] === "a" || name[i] === "A") {
        count++;
    }
}
console.log("The letter a or A is found " + count + " times in the string.");
 
